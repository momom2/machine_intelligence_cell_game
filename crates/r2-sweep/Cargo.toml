[package]
name = "r2-sweep"
version = "0.1.0"
edition = "2021"
description = "Headless cycle-sweep that answers risk R2 (triad cycle non-degeneracy)."

[[bin]]
name = "r2-sweep"
path = "src/main.rs"

[dependencies]
cell-core = { path = "../cell-core" }
